<div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
        <div>
    <script src="/chart/jquery-3.2.1.min.js"></script>
    <script>       

        function excel() {
            var tab_text = "<table border='2px'><tr bgcolor='#87AFC6'>";
            var textRange;
            var j = 0;
            tab = document.getElementById('InfoTable'); // id of table

            for (j = 0; j < tab.rows.length; j++) {
                tab_text = tab_text + tab.rows[j].innerHTML + "</tr>";
            }

            tab_text = tab_text + "</table>";
//            tab_text = tab_text.replace(/<A[^>]*>|<\/A>/g, ""); //remove if u want links in your table
//            tab_text = tab_text.replace(/<img[^>]*>/gi, ""); // remove if u want images in your table
//            tab_text = tab_text.replace(/<input[^>]*>|<\/input>/gi, ""); // reomves input params

            var ua = window.navigator.userAgent;
            var msie = ua.indexOf("MSIE ");
            
            if (msie > 0 || !!navigator.userAgent.match(/Trident.*rv\:11\./)) // If Internet Explorer
            {
                txtArea1.document.open("txt/html", "replace");
                txtArea1.document.write(tab_text);
                txtArea1.document.close();
                txtArea1.focus();
                sa = txtArea1.document.execCommand("SaveAs", true, "Say Thanks to Sumit.xls");
            } else //other browser not tested on IE 11
                sa = window.open('data:application/vnd.ms-excel,' + encodeURIComponent(tab_text));

            
            return (sa);

        }
    </script>
</div>
    <p align="right">
    <button class="btnExample" type="submit" value="Submit" onclick='excel();'><img src="/imgs/excel.png" width="18" height="18" alt=""/> Export to Excel</button>
    </p>
    
    <h1 class="page-header">Current Servers Owned by <b><%=ItemList[0].AccountName%></b></h1>
    <div class="table-responsive">
        <script src = "/chart/sorttable.js"></script>
        <table class="table table-striped sortable" id = "InfoTable">
            <thead>
                <tr>
                    <th>Server Name</th>
                    <th>Location</th>
                    <th>Phase</th>
                </tr>
            </thead>
            <tbody>
                <% if (ItemList.length > 0) { %>
                    <% for(i = 0; i < ItemList.length; i++){%>
                <tr>
                    
                    <td width = "35%"><a a href = "/Program/<%=ItemList[i].ProgramName%>/<%=ItemList[i].AccountName%>/<%=ItemList[i].ID%>"><%=ItemList[i].ProgramName%></a></td>
                    <td width = "20%"><a a href = "/Program/<%=ItemList[i].ProgramName%>/<%=ItemList[i].AccountName%>/<%=ItemList[i].ID%>"><%=ItemList[i].Region%></a></td>
                    <td width = "15%"><a a href = "/Program/<%=ItemList[i].ProgramName%>/<%=ItemList[i].AccountName%>/<%=ItemList[i].ID%>"><%=ItemList[i].Phase%></a></td>
                </tr>
                        <%}%>
                    <%}%>
            </tbody>
        </table>
    </div>
</div>